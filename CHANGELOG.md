# Changelog

## 0.2.4 — 2026-08-20

- MIT License 추가. 저작권자는 두 기여자 `sj10132`, `sxhx2009-crypto`.
  이전에는 `LICENSE`가 없어 공개 저장소인데도 법적으로 모든 권리 유보 상태라
  다른 사람이 복제·수정·배포할 수 없었다.
- `pyproject.toml`에 PEP 639 SPDX 표기(`license = "MIT"`,
  `license-files = ["LICENSE"]`)를 선언. 빌드된 wheel의 메타데이터에
  `License-Expression: MIT`가 실리고 `LICENSE`가 동봉되는 것을 확인했다.
- README와 업로드 가이드의 "라이선스를 아직 고르지 않았다"는 안내를 갱신.

## 0.2.3 — 2026-08-20

- 0.2.2의 overflow 방어가 불완전했던 것을 수정. 개별 값만 검사해서는 부족하다.
  값이 각각 표현 가능해도 두 곳에서 넘칠 수 있었다.
  - `_axis_initialization`의 `np.cumsum` — 누적 적분값이 넘침.
    재현: `cdtw_distance(np.tile([1e153,-1e153],100), [0.0,1e153])`
  - `_cell_cost_matrix`의 `start**2 + end**2` — 제곱 하나는 `sqrt(float64 최대)`
    안에 들어와도 **둘의 합**이 넘침. 재현: `cdtw_distance([0,1e154],[0,2e153])`
  둘 다 `inf` + `RuntimeWarning`을 반환했다. `_validate_cost_range`가 최대 높이
  `H`와 총 호 길이 `L`로 `2·H²`와 `L·H`를 검사해 명확한 `ValueError`를 낸다.
  상계이므로 실제로 계산 가능한 큰 값(`5e299`, `4.95e305`)은 막지 않는다.
- `estimated_error` 문서를 실제 동작에 맞춤. 창이 전부 0이면 마지막으로 값이
  움직인 폭을 보고하므로 "최근 창의 최대 변화"라는 설명이 틀렸다(무작위 60쌍 중
  4건에서 실제로 달랐다).
- `cdtw_adaptive`의 죽은 코드 `converged = bool(converged)` 제거.
- README에 노이즈 민감성 명시. 호 길이는 총변동이라 `1e-16` 크기의 반전도
  꼭짓점이 되어 셀 수와 실행시간을 늘린다.
- 테스트 27개 → 29개 (쌍 단위 overflow 차단, 큰 값 정상 계산).

## 0.2.2 — 2026-08-20

- `_as_curve`가 같은 방향으로 이어지는 연속 선분을 병합해 **꼭짓점만** 남긴다.
  곡선 자체는 그대로이지만 셀 수가 줄어 다음 세 가지가 동시에 해결됐다.
  - 공선점 재표본화가 **정확히 불변**이 됐다(무작위 200쌍 최대 상대편차
    `4.07e-3` → `0.0`). 검증 스위트의 재표본화 진단도 전부 `0.0`.
  - 같은 격자에서 값이 더 낮아졌다(예: `3.016090022` → `3.016089500`).
    인위적으로 쪼개진 셀 경계가 만들던 제약이 사라졌기 때문이다.
  - 스트레스 벤치마크 `21.0s` → `5.76s`, 피크 메모리 `33.6MiB` → `26.8MiB`.
- 입력 크기 상한 검사 추가. 비용이 높이의 제곱을 쓰므로 절댓값이
  `sqrt(float64 최대) ≈ 1.34e154`를 넘으면 `ValueError`를 낸다. 이전에는
  `cdtw_distance([0.0, 1e200, 0.0], [0.0, 1e200])`가 `RuntimeWarning` 16건과
  함께 `inf`를 반환했다.
- `cdtw_adaptive`가 최고 해상도를 두 번 계산하던 문제 수정. 경로를 루프 안에서
  바로 만들어 가장 비싼 동적계획을 한 번만 돈다.
- `cdtw_adaptive`의 평탄 구간 보호 강화. `grid_shape`가 그대로인 해상도 단계는
  수렴 증거로 세지 않고, 창 전체가 동일하면 `estimated_error`를 0이 아니라
  마지막으로 값이 움직인 폭으로 보고한다. 무작위 120쌍 기준 거짓 수렴
  3건 → 1건. 완전히 없어지지는 않으며 README에 명시했다.
- Richardson 외삽은 검토 후 **채택하지 않았다.** 관측 수렴 차수가 단순 입력에서만
  2이고 실제 곡선에서는 30.6/-33.6/0.35로 불안정하며, 평탄 구간에서는
  `estimated_error`를 정확히 0으로 보고해 문제를 되살린다.
- 한 해상도만 평가한 경우 `converged`를 `False`가 아니라 `None`으로 반환한다
  (`CDTWResult` 문서가 원래 명시하던 계약).
- `cdtw_adaptive`가 `return_path`/`normalized`의 타입을 검증한다.
- 모듈 docstring의 `O((n+m)^5)`를 논문 표기 `O(n^5)`로 정정.
- 테스트 18개 → 27개. 다중 셀 해석해(`[0,1,0,1]` 대 `[0,1]` = 0.5), 격자 세분
  단조성, 독립 격자-DP 상계, overflow 예외, adaptive 경로 비용 일치, 평탄 구간,
  단일 해상도, normalized/극단 격자, adaptive 인자 검증을 추가했다.

## 0.2.1 — 2026-08-20

- 매개변수 격자에서 곡선 꼭짓점을 앵커로 보존하도록 수정. 전체 호 길이 대비
  `32*eps`보다 짧은 선분이 병합돼 사라지면서 발생하던 두 가지 문제를 함께
  해결한다.
  - `RuntimeError: a curve vertex is missing from the parameter grid` 크래시.
    재현: `cdtw_distance([0.0, 1e-9, 1e6], [0.0, 1e6])`
  - 총 호 길이가 약 `1e-13` 이하인 곡선에서 모든 표본이 한 좌표로 무너져
    **조용히 `0.0`(또는 `inf`)을 반환**하던 오답. 재현:
    `cdtw_distance([0.0, 1e-13], [-1.0, -1.0])`
- `_break_indices`의 허용오차를 국소 좌표가 아니라 `_unique_sorted`와 같은
  전역 호 길이 스케일로 측정하도록 통일. 두 허용오차가 어긋나 있던 것이
  위 크래시의 직접 원인이었다.
- 두 곡선의 길이가 거의 같아 좌표계를 공유할 때, 공유 좌표계를 곡선별로
  잘라내어 격자가 항상 순증가하고 끝점이 정확하도록 보장.
- 공선점 재표본화 편차, 극단적 크기비, 미소 곡선에 대한 회귀 테스트 4종 추가
  (14개 → 18개).
- README에 공선점 재표본화 비불변성과 삼각부등식 미성립을 명시.
- 저장소를 평탄화해 `pyproject.toml`을 루트로 이동. 이전에는 패키지가 한 단계
  아래에 있어 문서에 적힌 `pip install "git+https://.../cdtw-python.git@..."`
  이 루트에서 빌드 설정을 찾지 못해 실패했다.
- `.github/workflows/tests.yml` 추가. README가 존재한다고 설명해 온 CI가 실제로는
  없었다. Python 3.10~3.14 단위 테스트와 예제·검증 스위트·패키지 빌드를 검사한다.
- `.gitignore` 추가 (`__pycache__/`, 빌드·coverage 산출물, `.venv/`). 추적 중인
  `validation/validation_results.json`이 무시되지 않도록 넓은 패턴은 넣지 않았다.
- 3.14 분류자 추가.

## 0.2.0 — 2026-08-19

- 셀 내부 닫힌형 비용을 사용하는 경계표본 CDTW 구현
- 시작·끝 대칭 격자로 공동 역순 불변성 회귀 수정
- `cdtw_adaptive`에 기본 3단계 안정화 창 적용
- 기본 512 MiB 보수적 메모리 가드 추가
- 14개 단위 테스트와 재현 가능한 전문 검증 자료 추가
- GitHub Actions 및 `pyproject.toml` 기반 설치·빌드 지원
